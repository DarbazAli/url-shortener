doctype html
html
    include ./head.pug
    body
        a(href="/") Home

        h1 URL SHORTENER
        
        include ./form.pug

        h3 New URL
        a(href=`${short_url}` target="_blank") #{short_url}


        include ./footer.pug